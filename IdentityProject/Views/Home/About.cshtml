@{
    ViewBag.Title = "About";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

<p>Use this area to provide additional information.</p>

@if (User.Identity.IsAuthenticated) {
    <div>You are logged in!</div>
}
else {
    <div class="warning">You aren't logged in!</div>
}